{% extends '@App/layout.html.twig' %}

{% block body %}
    <h1>Categories list</h1>
    {% for category in categories %}
        <a href="{{ path('product_by_category',{id: category.id}) }}"> Category name {{ category.id }}:{{ category.name }}</a>  </br>

    {% endfor %}
{% endblock %}

    {% block title %}Categories list{% endblock %}
    {% block stylesheets %}

    {% endblock %}
